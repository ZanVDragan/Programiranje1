import java.util.Scanner;
import java.util.Arrays;

public class UrejanjeTrojice {
    public static void main(String [] args) {
        Scanner sc = new Scanner(System.in);
        
        int[] pool = new int[3];
        
        pool[0] = sc.nextInt();
        
        pool[1] = sc.nextInt();
        
        pool[2] = sc.nextInt();
        
        Arrays.sort(pool);
        
        for (int i = 0; i < pool.length; i++) {
        
            System.out.print(pool[i]);
            
            //Zapiši presledek, razen pri zadnjem elementu.
            if (i < (pool.length - 1)) {
                System.out.print(" ");
            } else {
                System.out.println();
            }
        }
        
    }
}
